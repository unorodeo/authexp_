import { Button } from "@/components/ui/button";
import React from "react";

export const SignOut: React.FC = () => {
  return (
    <Button
      variant={"ghost"}
      size={"sm"}
    >
      Sign Out
    </Button>
  );
};
